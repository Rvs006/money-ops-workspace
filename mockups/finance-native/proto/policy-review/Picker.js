import React,{useCallback,useEffect,useLayoutEffect,useRef,useState} from 'react';
const variants=['Inline','Sheet','Complete'];
export default function Picker({current,setCurrent,replay}){const highlight=useRef(null),items=useRef([]);const [ready,setReady]=useState(false);const moveHighlight=useCallback(()=>{const el=items.current[current];if(el&&highlight.current){highlight.current.style.width=`${el.offsetWidth}px`;highlight.current.style.transform=`translateX(${el.offsetLeft}px)`}},[current]);useLayoutEffect(moveHighlight,[moveHighlight]);useEffect(()=>{window.addEventListener('resize',moveHighlight);return()=>window.removeEventListener('resize',moveHighlight)},[moveHighlight]);useEffect(()=>{requestAnimationFrame(()=>requestAnimationFrame(()=>setReady(true)))},[]);useEffect(()=>{const handler=e=>{const tag=e.target?.tagName;if(/^(INPUT|TEXTAREA|SELECT)$/.test(tag)||e.target?.isContentEditable||e.metaKey||e.ctrlKey||e.altKey)return;const n=parseInt(e.key,10);if(n>=1&&n<=variants.length)setCurrent(n-1);else if(e.key==='ArrowRight')setCurrent((current+1)%variants.length);else if(e.key==='ArrowLeft')setCurrent((current-1+variants.length)%variants.length);else if(e.key==='r'||e.key==='R')replay()};document.addEventListener('keydown',handler);return()=>document.removeEventListener('keydown',handler)},[current,replay,setCurrent]);return <nav className="proto-picker" data-ready={ready?'':undefined} data-position="top" aria-label="Prototype variants"><span ref={highlight} className="proto-picker-highlight" aria-hidden="true"/>{variants.map((name,index)=><button key={name} ref={el=>items.current[index]=el} className="proto-picker-item" data-active={current===index?'':undefined} aria-current={current===index?'true':undefined} onClick={()=>setCurrent(index)}>{name}</button>)}<span className="proto-picker-divider" aria-hidden="true"/><button className="proto-picker-item proto-picker-replay" aria-label="Replay animation (R)" onClick={replay}>↻</button></nav>}
